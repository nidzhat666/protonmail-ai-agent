[project]
name = "protonmail-ai-agent"
version = "0.1.0"
description = "AI-powered email assistant for ProtonMail via Proton Bridge"
requires-python = ">=3.14"
dependencies = [
    "openai>=1.40.0",
    "pydantic>=2.0.0",
]
